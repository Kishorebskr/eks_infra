apiVersion: v1
kind: Service
metadata:
  name: test-app-svc
  namespace: demo
spec:
  type: ClusterIP
  selector:
    app: test-app
  ports:
    - port: 80        # Service port exposed inside cluster
      targetPort: 3000  # ✅ must match containerPort in your Deployment

